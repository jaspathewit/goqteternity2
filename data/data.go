// Package contains the operations for the interaction with the mongodb.
package data

// Tile type constants
const (
	STATUS_UNKNOWN  byte = byte(0)
	STATUS_READY         = byte(1)
	STATUS_STARTED       = byte(2)
	TYPEOF_FINISHED      = byte(3)
)
